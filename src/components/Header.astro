---
import LanguageSwitch from './LanguageSwitch.astro';
import { withBase } from '../utils/paths';

interface Props {
  lang: 'pl' | 'en';
  plHref?: string;
  enHref?: string;
}

const {
  lang,
  plHref = withBase('pl/'),
  enHref = withBase('en/'),
} = Astro.props;

const aboutLabel = lang === 'pl' ? 'O projekcie' : 'About';
const aboutHref = withBase(`${lang}/about/`);

const glossaryLabel = lang === 'pl' ? 'Słownik' : 'Glossary';
const glossaryHref = withBase(`${lang}/glossary/`);
const homeHref = withBase(`${lang}/`);
---

<header class="site-header">
  <div class="site-header__inner">
    <a
      class="brand"
      href={homeHref}
      aria-label="AI Dev Notes"
    >
      AI Dev Notes
    </a>

    <nav
      class="site-nav"
      aria-label={lang === 'pl' ? 'Główna nawigacja' : 'Main navigation'}
    >
      <a href={aboutHref}>{aboutLabel}</a>
      <a href={glossaryHref}>{glossaryLabel}</a>

      <LanguageSwitch
        lang={lang}
        plHref={plHref}
        enHref={enHref}
      />
    </nav>
  </div>
</header>

<style>
  .site-header {
    width: 100%;
    margin: 0 auto;
    padding: 24px 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .site-header__inner {
    width: min(1080px, calc(100% - 40px));
    margin: 0 auto;
    padding: 24px 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .brand {
    color: inherit;
    font-size: 1.05rem;
    font-weight: 760;
    line-height: 1;
    letter-spacing: -0.02em;
    text-decoration: none;
  }

  .site-nav {
    display: flex;
    align-items: center;
    gap: 18px;
    color: #475569;
    font-size: 0.9rem;
  }

  .site-nav > a {
    min-height: 44px;
    margin-block: -13px;
    padding: 0 4px;
    display: inline-flex;
    align-items: center;
    color: inherit;
    text-decoration: none;
  }

  .site-nav > a:hover,
  .site-nav > a:focus-visible {
    color: #0f172a;
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  @media (max-width: 640px) {
    .site-header__inner {
      width: min(100% - 32px, 1080px);
      padding: 20px 0;
      gap: 12px;
    }

    .site-nav {
      gap: 8px;
      font-size: 0.86rem;
    }
  }
</style>
